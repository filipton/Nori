[package]
name = "nori-text"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "How Nori writes numbers: the platform's decimal style and Java's rounding, shared by every crate that words something"

[lib]
name = "nori_text"

[dependencies]
